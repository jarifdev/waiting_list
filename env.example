# MongoDB connection string (e.g. from MongoDB Atlas)
MONGODB_URI=mongodb+srv://obazaar:obazaar123@cluster0.74156u7.mongodb.net/mydb?retryWrites=true&w=majority

# Cloudinary (optional). If not set, images will be skipped.
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Optional: encryption salt for password hashing (this is a waitlist, not prod auth)
PASSWORD_SALT=changeme
